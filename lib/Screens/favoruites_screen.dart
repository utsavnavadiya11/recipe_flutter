import 'package:flutter/material.dart';

class FavoruiteScreen extends StatelessWidget {
  const FavoruiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favoruites!'),
      ),
    );
  }
}